class Installment {
    constructor(value, numOfInstallment) {
        this.value = value
        this.numOfInstallment = numOfInstallment
        this.paid = false
    }
}

module.exports = Installment