enum planetas { //definição de valores para certas palavras
    MERCURIO = 'mercurio',
    VENUS = 2,
    TERRA = 'terra',
    MARTE = 3
}

planetas.MERCURIO
planetas.VENUS
planetas.TERRA
planetas.MARTE

enum Roles { //aplicação real
    ADMIN = 'admin',
    USER = 'user'
}